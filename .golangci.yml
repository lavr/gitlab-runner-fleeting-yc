linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - gofmt
    - misspell

linters-settings:
  govet:
    check-shadowing: true

issues:
  exclude-rules:
    - path: _test\.go
      linters: [errcheck]
